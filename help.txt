Instalar Nix:
    1. sh <(curl --proto '=https' --tlsv1.2 -L https://nixos.org/nix/install) --daemon
    2. curl --proto '=https' --tlsv1.2 -sSf -L https://install.determinate.systems/nix | sh -s -- install (RECOMENDADO)

Instalar direnv (não obrigatório):
    Fedora: sudo dnf install direnv
    Ubuntu: sudo apt install direnv

Comandos:
    shell.nix -> nix-shell
    flake.nix -> nix develop

Comandos com zsh:
    nix-shell --run zsh
    nix develop -c zsh

Verificar se esta no shell do nix:
echo $IN_NIX_SHELL -> impure

URLS:
Pesquisar Pacotes:
https://search.nixos.org/packages

Repos para uso:
https://github.com/NixOS/nixpkgs
github:nixos/nixpkgs/nixos-unstable
github:nixos/nixpkgs/nixos-25.11 (stable)

https://flakehub.com/flakes
https://flakehub.com/flake/NixOS/nixpkgs?view=usage

Meus Repos:
https://github.com/joaopfusco/nix-config
https://github.com/joaopfusco/nix-course
